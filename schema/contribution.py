"""Contribution record -- governance as data structure (CARE).

Every contribution (clip, label, correction) carries who made it and under what terms.
No write path may bypass attribution.
"""

# TODO(agent): ContributionRecord dataclass --
#   contributor identity, attribution, machine-readable terms (authority to control,
#   collective benefit), the parse corrected, timestamp, source collection.
# TODO(agent): consent/terms model (per-collection; what may be trained on, what flows back).
